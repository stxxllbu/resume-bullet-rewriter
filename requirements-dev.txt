# Development / CI only. Prefer: pip install -e ".[dev]" (see pyproject.toml).
# This file is kept for convenience; dev deps are defined in pyproject.toml [project.optional-dependencies].
pytest>=7.0
